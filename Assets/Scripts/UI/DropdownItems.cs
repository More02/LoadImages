namespace UI
{
    /// <summary>
    /// Перечисляемый тип с видами загрузки и отображения изображений
    /// </summary>
    public enum DropdownItems
    {
        Allatonce = 0,
        Onebyone = 1,
        Whenimageready = 2
    }
}